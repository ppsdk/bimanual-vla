# 8_3_64eps 实机安全阈值基线

统计日期：2026-08-03

数据集：`8_3_64eps`（83 episodes，18,034 frames，20 Hz，legacy delivery v2）

该数据集的 action 是相邻 20 Hz 帧之间的 EEF one-step delta，夹爪是下一帧绝对 closed fraction。以下统计用于确定 `bimanual_vla/deployment/client.py` 的默认 delivery 安全门；CLI 仍可针对新任务收紧。

## 20 Hz 单步动作分布

| 指标 | P95 | P99 | P99.9 | 最大值 | 新默认上限 |
|---|---:|---:|---:|---:|---:|
| EEF 平移范数 | 0.02054 m | 0.03078 m | 0.03909 m | 0.04830 m | **0.05 m/command** |
| EEF 旋转范数 | 0.06485 rad | 0.09856 rad | 0.13049 rad | 0.15766 rad | **0.18 rad/command** |
| 夹爪开口比例变化 | 0.0830 | 0.1580 | 0.2169 | 0.2610 | **0.30/command** |

旧默认平移阈值 `0.015 m` 会阻断约 9.78% 的真实数据帧，因此对该数据集明显过严。新默认值覆盖实测最大值并保留小幅数值/融合裕量，但仍逐个 20 Hz 控制命令重新检查，不允许 chunk 或轨迹融合绕过安全门。

## 工作空间分布与默认边界

| 轴 | 实测最小 | 实测最大 | 新默认边界 |
|---|---:|---:|---:|
| x | -0.03815 m | 0.27987 m | **[-0.05, 0.30] m** |
| y | 0.02183 m | 0.47802 m | **[0.01, 0.50] m** |
| z | 0.14706 m | 0.50322 m | **[0.14, 0.52] m** |

这些边界只覆盖当前任务/标定附近的数据包络，不是 Piper 的机械极限。更换桌面、基坐标、工具、任务或标定后，必须重新统计并通过 CLI 显式设置。

## 连续推理/控制约束

- 采集与模型动作频率：20 Hz。
- 控制频率：20 Hz。
- 异步推理启动频率：约 4 Hz（推理和传输实测约 200 ms）。
- 推理期间继续执行旧 chunk。
- 每个目标带 `t_obs + (i+1)×0.05 s` 时间戳；新 chunk 到达后按实际
  `execution_time` 动态丢弃过时目标，并用默认 3 步位姿融合切换。
- 夹爪不参与 old/new 轨迹线性融合，独立使用低通、迟滞、step limit 和连续预测确认。
- 每个融合后的 absolute target 仍必须通过上述 workspace、平移、旋转、夹爪、反馈新鲜度和 Piper 状态检查。
