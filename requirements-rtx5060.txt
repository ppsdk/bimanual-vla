# RTX 5060 common edge-server dependencies (x86_64/Linux).
# Install a CUDA-enabled PyTorch build compatible with the host driver first.
# Choose exactly one model environment after installing this file:
#   requirements-rtx5060-pi.txt
#   requirements-rtx5060-smolvla.txt
# Pi/OpenPI and SmolVLA require incompatible Transformers versions and must
# not be installed into the same virtual environment.

numpy==1.26.4
scipy==1.15.3
PyYAML==6.0.3
safetensors>=0.4.0
opencv-python-headless==4.10.0.84
Pillow>=10.0.0
websockets==15.0.1
openpi-client==0.1.2
