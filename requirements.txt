# Core runtime dependencies for collection, data processing, the Dashboard,
# and the robot-side RTC client. Verified against Python 3.10 on Ubuntu 22.04.
# The OpenPI training/server environment is installed separately; see
# docs/INSTALLATION.md.

# Numerical and data stack
numpy==1.26.4
scipy==1.15.3
pandas==2.3.3
pyarrow==25.0.0
PyYAML==6.0.3

# Images, video, and LeRobot v2.1 datasets
opencv-python==4.10.0.84
opencv-python-headless==4.10.0.84
Pillow==12.3.0
av==15.1.0
lerobot==0.3.3
torch==2.7.1
torchvision==0.22.1
torchcodec==0.5
safetensors==0.8.0

# Piper hardware and Policy transport
piper_sdk==0.6.2
python-can==4.6.1
websockets==15.0.1
openpi-client==0.1.2

# Dashboard
Flask==3.1.3
Werkzeug==3.1.8
